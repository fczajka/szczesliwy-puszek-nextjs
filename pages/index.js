import Image from "next/image";
import { gallery } from "../content";

const Home = () => {
    return (
        <div className="w-full h-screen-base sm:h-screen-sm lg:h-screen-lg"></div>
    );
};

export default Home;

export enum ButtonTypes {
    button = "button",
    submit = "submit",
    reset = "reset",
    anchor = "anchor",
}

export enum Colors {
    blue = "babyBluue",
    pink = "royalPink",
}
